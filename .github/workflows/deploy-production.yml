name: VOKASI Deploy Production

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/vokasi-backend:latest
            ghcr.io/${{ github.repository }}/vokasi-backend:${{ github.sha }}

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/vokasi-frontend:latest
            ghcr.io/${{ github.repository }}/vokasi-frontend:${{ github.sha }}

  deploy-via-ssh:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: ${{ secrets.VOKASI_PROD_SSH_HOST != '' && secrets.VOKASI_PROD_SSH_USER != '' && secrets.VOKASI_PROD_SSH_KEY != '' }}
    environment: production
    steps:
      - name: Deploy to production host
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VOKASI_PROD_SSH_HOST }}
          username: ${{ secrets.VOKASI_PROD_SSH_USER }}
          key: ${{ secrets.VOKASI_PROD_SSH_KEY }}
          script: |
            cd ${{ secrets.VOKASI_PROD_APP_DIR }}
            docker compose -f docker-compose.production.yml pull
            docker compose -f docker-compose.production.yml up -d
